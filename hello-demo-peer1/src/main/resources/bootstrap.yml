server:
  port: 8002
spring:
  application:
    name: helloclient
  cloud:
    config:
      name: helloclient
      uri : http://localhost:5000/
      username: nick
      password: 123456
#      discovery:
#        service-id: config-server
#        enabled: true
  profiles:
    active: dev


#eureka:
#  client:
#    enabled: false
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10000/eureka/
